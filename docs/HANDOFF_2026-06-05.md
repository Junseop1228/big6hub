# Big6Hub — Session Handoff Document

**Date:** 2026-06-05
**Repo:** https://github.com/Junseop1228/big6hub
**Deadline:** Final Delivery 2026-06-09 (4 days remaining)
**Next exam:** Final exam today at 10:00 AM (Lectures 9–13)

---

## 1. Ground Rules (Always Follow)

Before writing any code or making commits:
1. Read `docs/PREMISE.md` — tech stack guardrails
2. Read `docs/ARCHITECTURE.md` — folder structure and request lifecycle
3. Read `docs/GIT_POLICY.md` — branch/commit/PR conventions
4. All repo content in English (code, comments, commits, issues, .md files)

---

## 2. Team

| Name | Role | GitHub |
|---|---|---|
| Junseop Kim (lead) | Backend API + DB | @Junseop1228 |
| Hyoungdo Kim | Backend Testing + CI + Docs | TBD |
| Seongbin Lee | Frontend UI | @yeonlimee2 |
| Geon Kim | Frontend Auth | TBD |

---

## 3. Tech Stack

- Backend: Node.js + Express
- DB: SQLite (sqlite + sqlite3)
- Auth: JWT + bcrypt
- Frontend: Vanilla HTML / CSS / JS
- Testing: Jest + Supertest
- CI: GitHub Actions (triggers on push/PR to main)
- API Docs: Swagger / OpenAPI

---

## 4. Git Workflow

```
1. Open Issue on GitHub (before any work)
2. git checkout dev && git pull origin dev
3. git checkout -b feature/{name}/{task}
4. Write code → test locally
5. git add {files} && git commit -m "type: summary"
6. git push -u origin feature/{name}/{task}
7. Open PR on GitHub: feature → dev
   - Title follows Conventional Commits
   - Description: Summary / Closes #{issue} / Checklist
   - Labels, Milestone, Assignee, Reviewer
   - Move Project Board → In Review
8. Team member reviews → Approve → Merge
   - Issue auto-closes via "Closes #N"
   - Move Project Board → Done
9. git checkout dev && git pull origin dev (repeat)
```

Branch naming: `feature/{name}/{task}`, `fix/{name}/{issue}`, `docs/{name}/{topic}`
Commit types: feat, fix, docs, test, refactor, chore, style

---

## 5. Project Structure

```
big6hub/
├── app.js              ✅ Express entry point
├── db.js               ✅ SQLite connection + table creation
├── seed.js             ❌ Not yet created
├── openapi.yaml        ❌ Not yet created
├── routes/
│   ├── teams.js        ✅ GET /api/teams
│   ├── auth.js         ❌
│   ├── seasons.js      ❌
│   ├── players.js      ❌
│   └── favorites.js    ❌
├── controllers/
│   ├── teamsController.js  ✅
│   └── (others)            ❌
├── models/
│   ├── teamsModel.js   ✅
│   └── (others)        ❌
├── middleware/         ❌ All empty
├── public/             🟡 Seongbin's branch (not merged yet)
├── data/               ❌ Empty
├── tests/              ❌ Empty
└── docs/               ✅ PREMISE, ARCHITECTURE, GIT_POLICY, PDF
```

---

## 6. Current Git State

**Branches:**
- `main` — initial structure only, CI failing
- `dev` — 12 commits ahead of main
- `feature/junseop/teams-api` — pushed, PR pending (#13)
- `feature/seongbin/frontend-skeleton` — 16 commits, NO PR yet

**Merged PRs:**
- PR #3: feat: app.js ✅
- PR #5: feat: db.js ✅

**Open Issues (8 total):**
- #13 [feat] Implement GET /api/teams — PR pending
- #1, #6–#12 — all Seongbin's frontend issues (branch exists, no PR)

**Milestones:**
- W1: Backend Skeleton ✅ (100%)
- W2: Auth + Frontend 🔴 (0%, due today Jun 5)
- W3: Integration 🔴 (0%, due Jun 8)
- Final Delivery 🔴 (Jun 9)

---

## 7. Completed This Session

### Code
- `app.js` — Express server, middleware (helmet, cors, express.json, static), initDb call
- `db.js` — SQLite connection, 7 tables: teams, seasons, players, trophies, managers, users, favorites
- `models/teamsModel.js` — getAllTeams() → db.all('SELECT * FROM teams')
- `controllers/teamsController.js` — getTeams(req, res) → getAllTeams() → res.json()
- `routes/teams.js` — router.get('/', getTeams) → module.exports = router
- `app.js` updated — app.use('/api/teams', require('./routes/teams')) uncommented
- Tested: GET http://localhost:3000/api/teams → returns []

### Docs
- docs/PREMISE.md, ARCHITECTURE.md, GIT_POLICY.md, PDF
- README.md
- .gitignore, .env.example
- ci.yml (GitHub Actions, Node 24)

### GitHub Setup
- Repo: https://github.com/Junseop1228/big6hub (Public)
- Branches: main (protected: 1 approval required), dev
- Labels: feature, bug, test, docs, frontend, backend, blocked
- Milestones: W1–W3, Final Delivery
- Projects: Big6Hub Sprint Board (Kanban)
- Collaborator: yeonlimee2

---

## 8. Immediate Next Steps (Priority Order)

### Today (before/after exam)

1. **Merge PR #13** (teams-api) → dev
2. **Contact Seongbin** → ask her to open PR: feature/seongbin/frontend-skeleton → dev
3. **seed.js** — 6 Big 6 teams initial data (needed for demo)
4. **auth routes** — register, login (Lecture 12 content):
   - models/usersModel.js
   - controllers/authController.js
   - routes/auth.js
   - middleware/requireAuth.js

### Jun 6–8
5. Remaining API routes: players, seasons, favorites
6. Frontend ↔ Backend connection (public/js/api.js)
7. tests/ — Jest unit + Supertest API tests (Lecture 13)
8. openapi.yaml — Swagger docs
9. CI fix: add dev branch trigger to ci.yml
10. README final update (deployed URL, AI disclosure)

### Jun 9
11. dev → main PR (all team members confirm)
12. Git tag: v1.0.0
13. Final submission

---

## 9. Junseop's Learning Style & Approach

**How he learns best:**
- Concept → intuition (analogy first) → code → quiz
- Needs to understand WHY before HOW
- Asks sharp clarifying questions (e.g., "why `../db`?", "what's `{ open }`?")
- Prefers writing code himself with guidance, not copy-paste
- Gets confused when similar terms collide (e.g., `require` vs `req`)

**Coaching pattern that worked:**
1. Ask "what do you think this file should do?" before explaining
2. Give small hints when stuck, not full solutions
3. Issue a quiz after each Issue is closed
4. Explain errors by tracing back to the concept (식당 비유 worked well)

**Concepts mastered this session:**
- require / module.exports / destructuring
- process.env + dotenv
- Express middleware chain (app.use, next())
- Router vs direct handler (delegation vs direct)
- req.body, req.params, res.json(), res.status()
- async/await + try/catch pattern
- sqlite vs sqlite3 (wrapper pattern)
- db.exec / db.all / db.get / db.run differences
- Model → Controller → Routes layering
- throw in model vs res.json in controller (식당 비유)

**Concepts still shaky:**
- module.exports = x vs module.exports = { x } (confused once)
- Full Request Lifecycle end-to-end intuition (improving)

---

## 10. Key Patterns to Keep Using

```javascript
// Model pattern (Lecture 11)
const { getDb } = require('../db');
async function getAllTeams() {
  try {
    const db = getDb();
    return await db.all('SELECT * FROM teams');
  } catch (err) {
    throw err;
  }
}
module.exports = { getAllTeams };

// Controller pattern (Lecture 10)
const { getAllTeams } = require('../models/teamsModel');
async function getTeams(req, res) {
  try {
    const teams = await getAllTeams();
    res.json(teams);
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
}
module.exports = { getTeams };

// Router pattern (Lecture 10)
const express = require('express');
const router = express.Router();
const { getTeams } = require('../controllers/teamsController');
router.get('/', getTeams);
module.exports = router;
```

---

## 11. Upcoming: Auth Flow (Lecture 12)

Next major topic. Key concepts to cover:
- bcrypt.hash() / bcrypt.compare()
- jsonwebtoken.sign() / jwt.verify()
- requireAuth middleware pattern
- req.headers.authorization parsing
- JWT stored in localStorage on client

```
POST /api/auth/register → hash password → store user → return 201
POST /api/auth/login    → find user → compare hash → sign JWT → return token
GET  /api/teams         → public (no auth needed)
POST /api/players       → requireAuth → requireAdmin → controller
```

---

*Generated: 2026-06-05 | Next session start: after final exam*
